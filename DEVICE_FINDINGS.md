# RetroLens device findings

## Proven on the ILCE-5100 before the native RetroLens build

- The inherited Sony lifecycle baseline opens `CameraEx`, starts normal preview, preserves manual autofocus and still capture, and releases cleanly.
- `CameraSequence` accepted 640×480, rate 30000, format 256, one queued frame, and a 262144-byte JPEG buffer on this same camera.
- Analytical payloads were valid JPEGs of roughly 55–83 KiB and arrived at about 9–10 FPS.
- Every observed `DeviceMemory` was released, and the proven stop order was worker, sequence, native consumer, then `CameraEx`.
- Repeated `takePicture(null,null,null)` calls worked with a guarded `cancelTakePicture()` around 418–428 ms later while analytical preview remained active.

Those results came from the source WaveSnap/PMCADemo baseline. They justify the API and lifecycle choices but do not prove RetroLens rendering or output files.

## RetroLens evidence on 2026-07-22

- Renamed recovery APK installed successfully as package `io.pihda.retrolens`.
- Final native-engine APK SHA-256 `435f93762f90740ece95377bf34b8250c2a6aa396da8803b7afcd89e3a07f9e1` installed successfully and contains only `lib/armeabi/libretrolens.so`.
- Host ASan/UBSan tests pass for all 70 preset graphs, tiny and odd dimensions, deterministic state, JPEG writing, JSON escaping, performance decisions, and AVI finalization.
- No `RETROLENS/LOG.TXT` existed when the camera was reconnected, so physical launch, filtered output, controls, capture derivative, and Retro Clip recording remain unverified after the latest install.
- A fully black display with working physical Sony shutter was observed from the earlier native build. New 5–6.6 MB Sony originals appeared on the card, confirming camera/shutter operation independently of the failed effect display.
- No `RETROLENS/` directory was produced by that run, even though Java session logging precedes native and camera startup. The exact installed/selected package or early failure point therefore remains unresolved.
- Stability build `stability-20260722-a`, final APK SHA-256 `f535468af3867b5139c0a6700b932fc3f1d0dc77c3d684ed0d6595cebb84d95c`, builds and verifies. An earlier stability iteration installed successfully; final-install and physical results are pending. It adds a synchronous surface probe and visible fallback, uses the real 80×60 reduced grid, renders on events, batches logs, shortens teardown bounds, and disables Retro Clip/processed derivatives.
- Physical testing of `stability-20260722-a` still showed a pure black screen. The shutter worked inside RetroLens and the Movie button had no visible effect. After exiting, the next shutter press in Sony's normal camera application remained at `Writing memory` with the red media LED continuously active; battery removal was required. This is a severe lifecycle regression, so that build is retired from hardware testing.
- The card had ample free space when inspected afterward. No `RETROLENS/` directory or log existed, so external app output did not fill the card and cannot explain the wedge.
- Static review found two concrete risks in the retired build: it forced high display color depth despite the proven camera apps using low depth, and it released `CameraEx` even after reporting that the CameraSequence worker had failed to stop. Its opaque second SurfaceView could also cover both normal preview and Java fallback after a successful initial surface probe followed by a later render failure.
- Safety build `safe-preview-20260722-b`, APK SHA-256 `c71655c7f71dff4c107954fc09c1478494f3cc61fd2528b79c74b0015cc14b63`, passed both physical test phases. The user confirmed normal preview/capture and clean post-exit normal-camera operation. Six new originals (`DSC05026.JPG` through `DSC05031.JPG`) were confirmed on the card at regular intervals, and no `RETROLENS/` directory was created. No `Writing memory` wedge, persistent media LED, or battery removal recurred.
- The proven safety APK is preserved as `releases/RetroLens-1.0.0-safe-preview.apk` with its checksum.
- Native display probe `native-probe-20260722-c`, APK SHA-256 `cb7b6cd9658395e6e1d5c7ea98d1225a7a1cdb16e92762bce4ea3c49be99d87f`, showed the expected pattern and `NATIVE DISPLAY OK`. The user reports that all repeated lifecycle/capture phases passed. `DSC05032.JPG` is the only new original independently visible from the latest mounted-card inspection, so the repeated write sequence is user-confirmed rather than fully reconstructed from card timestamps.
- Thread probe `thread-probe-20260722-d`, APK SHA-256 `9bdaddbcea606cf74c0b14b7f4b5aa966a733f2f0f48f9ea9366a578649fc4bb`, adds one fixed 256×144 RGB565 offscreen worker at 8 FPS. The user reported everything working and observed its counter advancing from frame 10 to frame 26. `DSC05033.JPG` independently confirms one new Sony original from that session. Twenty repeated worker lifecycle tests pass under ASan/UBSan and the full suite passes ThreadSanitizer.
- Sequence probe `sequence-probe-20260722-e`, APK SHA-256 `000bc1d559b99c32768b598df19a469d4217ff76b5fdbdc03d6764e7ac8d3802`, passed its complete hardware gate. The user confirmed the 30-second metrics run, repeated exit and post-exit normal-camera captures, capture inside RetroLens, autofocus/controls, and no persistent red LED or `Writing memory` wedge. Exact visible FPS and JPEG-size values were not separately transcribed, so only the lifecycle result is promoted.
- Filter-panel probe `filter-panel-probe-20260722-f`, APK SHA-256 `ed67240f922358cda6ad848d1453e49d6bdedd9b58f53a4c9209cedb34541e04`, adds one fixed native compressed slot, shared reduced PicoJPEG decode to 80×60, fixed Olive Pocket processing, bounded temporal history, and a letterboxed 256×144 filtered panel with compact metrics. Full-screen output, files, derivatives, settings, recorder allocation, and video remain disabled. Host ASan/UBSan and ThreadSanitizer tests plus the legacy API-10/armeabi build, APK verification, and physical installation pass. The user observed moving Olive Pocket output but correctly reported that the 4:3 image occupied only about 75% of the 16:9 panel; the remaining static area came from letterboxing.
- Style-panel probe `style-panel-probe-20260722-g`, APK SHA-256 `75945f5c7d256fd68ab2a2eaa28b7d452c8e539d61313592039e48045f952b7b`, changes the surface to 240×180 and draws diagnostics over the moving image. ASan/UBSan, ThreadSanitizer, legacy build, APK verification, and physical installation pass. The user then confirmed moving Olive Pocket output but reported that only the top-left portion of the filter panel updated, approximately one quarter of the camera display; the rest remained static black or olive. Therefore the intended exact 3× fill is not physically proven and the display-geometry defect remains open.
- The user explicitly requested that the panel-fill defect be left unresolved while the remaining application is completed as photo-only. The next candidate keeps essential controls within the known visible top-left 120×90 region, enables all 70 presets, per-preset controls, atomic 320×240 derivatives, sidecars, settings, favorites/recent state, and a bounded app gallery. It does not link the retired video runtime. None of those newly enabled file/UI behaviors are promoted to hardware-proven status until the candidate is tested on the camera.
- Photo-runtime candidate APK SHA-256 `03e7af6e535fc87d8d3ffefa27e4dc68bf0f930c11bc518c2fc577e9c126839e` passes ASan/UBSan, ThreadSanitizer, the clean NDK r14b/API-10 build, signing and APK-content verification. Sony-PMCA-RE installed it successfully on the connected ILCE-5100 on 2026-07-22. This proves package transport and acceptance only; launch, derivative files, gallery behavior, controls, and post-exit lifecycle remain untested for this candidate.
- The user subsequently confirmed that the photo-runtime candidate displays moving effects in its small panel, but the processed finished image was not visibly presented when opening the gallery. This confirms launch and live filtered output only; it does not prove that a derivative transaction completed or that the gallery index contained an entry because no log/card inspection accompanied the observation.
- Fullscreen-photo candidate `fullscreen-photo-20260722-i`, APK SHA-256 `71287901047901db7fda5a304c2bbf7f10ad585ffa2d703287b136fa5aa9bb3d`, changes the overlay layout to full-screen, stops requesting a 240×180 native window, center-crop scales the logical compositor into every pixel of the actual locked buffer, immediately preloads a durable processed save into the newest gallery slot, and renders the selected processed thumbnail as the gallery background. ASan/UBSan, ThreadSanitizer, the clean NDK r14b/API-10 build, signing verification, and `armeabi` APK-content checks pass. Per user instruction, this candidate was packaged but not installed; all physical behavior remains untested.
- After the user reported that only normal photos appeared, the mounted card was inspected read-only. Sony originals were present through `DCIM/10160722/DSC05050.JPG`; `PMCADEMO/LOG.TXT` and `WAVESNAP/LOG.TXT` existed, but there was no `RETROLENS/` tree or `RETROLENS/LOG.TXT`. This proves the tested RetroLens runtime did not complete even directory creation and means the missing processed files were a storage-initialization failure, not merely a Sony gallery visibility issue.
- Storage-repair candidate `storage-repair-20260722-j`, APK SHA-256 `0f2e687f7192e804972b923120c80e38b30edf788a944d2fb25d117c759d26ed`, adds a Java write/sync/rename probe, one canonical root shared by logging and native output, explicit native storage/write states, stale-transaction cleanup, validated index loading, and bounded missing/corrupt-index reconstruction from complete derivative sets. Host ASan/UBSan, ThreadSanitizer, and the clean API-10/armeabi APK suite pass. It has not been installed or physically tested per user instruction.
- The user physically tested the storage-repair UI and observed `SONY SAVED RETRO STORAGE OFF`; Playback still showed no RetroLens photos. The user also confirmed the persistent Java `RETROLENS / PHOTO ENGINE` header covered the native filter name. No new card log was available, but static review found that storage was probed only once during `onCreate`, earlier than the working apps access it, and that Java/native sync failures or unknown native free-space results could disable an otherwise writable legacy card.
- Storage/UI candidate `storage-ui-fix-20260722-k`, APK SHA-256 `172e68a7a12f9d950db53fb39c235c79991cc83b28d2501b6ee7736c3fb2d4a8`, moves card probing after resume with three bounded attempts, matches the working apps' absolute Environment path, verifies write/close/rename/read-back, tolerates only unsupported sync errors, and reports precise native storage states. It removes steady Java chrome, uses a one-shot first-processed-frame ready callback, auto-hides native camera bars after two seconds, adds a focus reticle, and expires routine messages. ASan/UBSan, ThreadSanitizer, clean API-10/armeabi build, signing, and APK-content verification pass. Per the standing instruction it was packaged but not installed; hardware behavior remains untested.
- The user physically tested the storage/UI flow and reported that the full startup panel never closed. Physical shutter capture still worked, while the filtered image was not visible. Static review confirmed that the splash closed only after native `processedFrames > 0`, while runtime creation waited for the Java card probe and analytical polling waited for a successful opaque-surface post. This coupled optional storage, surface, decode, and UI readiness into one failure-prone startup gate.
- Startup-recovery candidate `startup-recovery-20260722-l`, APK SHA-256 `fd495bfbde76238aa071efff56fb0c742da3294c658c7d0b5d22cfab49205592`, breaks that gate. It creates the native processor hidden as soon as Sony preview is ready, starts CameraSequence from native-runtime readiness, polls processing without a surface, and reveals the opaque surface only after the first filtered frame. The full splash reduces to a compact Sony-preview fallback after 800 ms and becomes `GONE` after the first successful filtered post. Photo storage initialization and write/rename/read-back verification now run on the existing bounded native photo worker, with an explicit initializing state. ASan/UBSan, ThreadSanitizer, formatting, clean API-10/armeabi build, signing, and APK-content verification pass. It has not been installed or physically tested.
- Following delivery of the startup-recovery candidate, the user reported `RETRO FOLDER FAILED`. Mounted-card inspection again found PMCADemo and WaveSnap logs but no `RETROLENS/` directory or log. This physically confirms that live application execution reached native storage state 3 while directory creation still failed; exact on-device errno was not transcribed.
- Storage-gate candidate `storage-gate-20260722-m`, APK SHA-256 `9638ec4ec57fa4087f7cd9fd7317fb4c748ee3c99120f53181f532f4d39a7c79`, delays native `PhotoStore` initialization behind five bounded Java `File.mkdirs()` and write/rename/read-back attempts. Preview and filtering remain independent, storage-pending shutdown is bounded, and diagnostics include Java attempt count. ASan/UBSan, ThreadSanitizer, Java 6 compilation, and the API-10/armeabi APK build pass. It is not installed or physically tested.
- The latest PMCADemo log showed many consecutive Boot/Exit receiver messages while navigating the Sony application menu. PMCADemo and LegacyAlphaRemote receiver behavior is being audited separately and has not been changed.

## Open hardware checks

1. Confirm the filtered preview covers the entire camera display with no static black/olive quadrants, stretching, or untouched old panel.
2. Traverse multiple categories and confirm all navigation leaves the full-screen filtered image responsive.
3. Adjust Intensity, Contrast, Grain, and Motion, exit/reopen, and confirm the selected preset and values persist.
4. Take one photo inside RetroLens, wait for the media LED to stop, and confirm the Sony original plus a separate 320×240 file and JSON under `RETROLENS/PHOTOS/`.
5. Open Playback and confirm the newest processed image fills the gallery screen with its preset and `PROCESSED 320X240` label. Test navigation and delete confirmation, then confirm the Sony original is untouched.
6. Open and exit five times, then take a normal-camera photo after each exit. Confirm no persistent red LED or `Writing memory` state.
7. Confirm the Movie button only reports photo mode and creates no `CLIPS` directory.
8. Collect `RETROLENS/LOG.TXT` and visible Fn diagnostics. Stop immediately on buffer imbalance, a frozen frame counter, persistent media LED, or `Writing memory`; reinstall the preserved safety APK before further diagnosis.
9. Confirm the full splash disappears or reduces within 800 ms even with no filtered frame, and that the opaque native surface appears only when a moving filtered image is ready.

Visible metrics, user observations, log entries, and independently inspected card files must remain distinguished.
